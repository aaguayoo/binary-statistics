#define _USE_MATH_DEFINES
#include <cmath>

using namespace std;

void plot(const vector<double>& hist, long bins, string color, double alpha, bool density, bool cumulative, string parameter, string units, string save_file);
